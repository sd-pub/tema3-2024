name: Checker

on: 
  push:
  workflow_dispatch:

jobs:
  checker:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/sd-pub/tema3:latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run tests
        run: |
          pwd
          ls
          ls checker/
          hw_checker --legacy